{
  "name": "http-server",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b",
    "dev": "yarn run build && yarn run start",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@repo/db": "*",
    "@repo/typescript-config": "*",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  }
}
